import { VncurrencyPipe } from './vncurrency.pipe';

describe('VncurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new VncurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
